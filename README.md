Run web tests: `mvn test -P web`  
Run native tests: `mvn test -P native`  
To run tests related with EPAM Cloud Mobile Service your need your own authentication token. Usage: *run test command* -Dtoken=token_value